# Bootstrap

Confirm repository access, read `AGENTS.md`, inspect `study.config.yml`, and identify the requested intake provider. Never overwrite an existing generated path without comparing changes. Work on a feature branch and prefer a pull request.
